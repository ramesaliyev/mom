import React from 'react';
import Typography from '@material-ui/core/Typography';

const ForgotPassword = props => (
  <Typography>{'ForgotPassword Page'}</Typography>
);

export default ForgotPassword;